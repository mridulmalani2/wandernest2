# ========================================================================
# WANDERNEST PRODUCTION ENVIRONMENT VARIABLES
# ========================================================================
# This template is for deploying to Vercel (or similar platforms)
# Copy this file and fill in the actual values before deployment
#
# REQUIRED vs OPTIONAL breakdown:
# - REQUIRED: App will not function without these
# - OPTIONAL: App will work but with reduced functionality or fallbacks
# ========================================================================

# ========================================================================
# 🔴 REQUIRED: DATABASE CONFIGURATION
# ========================================================================
# PostgreSQL connection string (REQUIRED for app to function)
#
# For Vercel Postgres:
#   1. Go to your Vercel project → Storage → Create Database → Postgres
#   2. Copy the "POSTGRES_URL" or "DATABASE_URL" from the .env.local tab
#   3. Paste it here
#
# For external provider (Supabase, Neon, Railway, etc.):
#   Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
#
DATABASE_URL="postgresql://..."

# ========================================================================
# 🔴 REQUIRED: GOOGLE OAUTH CREDENTIALS
# ========================================================================
# These are REQUIRED for both student AND tourist authentication
# Students must use academic emails, tourists can use any Google account
#
# Setup instructions:
#   1. Go to: https://console.cloud.google.com/apis/credentials
#   2. Create a new project (or use existing)
#   3. Enable "Google+ API" or "Google Identity Services"
#   4. Create OAuth 2.0 Client ID (Web application)
#   5. Add authorized redirect URIs:
#      - https://your-domain.vercel.app/api/auth/callback/google
#   6. Copy the Client ID and Client Secret below
#
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ========================================================================
# 🔴 REQUIRED: NEXTAUTH CONFIGURATION
# ========================================================================
# NextAuth secret (REQUIRED for session security)
# Generate a random secret with: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
#
NEXTAUTH_SECRET=""

# NextAuth URL (REQUIRED - your production domain)
# This should be your actual deployment URL (without trailing slash)
# Example: https://wandernest.vercel.app
#
NEXTAUTH_URL=""

# ========================================================================
# 🟡 OPTIONAL: EMAIL CONFIGURATION
# ========================================================================
# If these are NOT set:
#   - App will continue to work normally
#   - Emails will be logged to server console instead of being sent
#   - Users will NOT receive booking confirmations, notifications, etc.
#
# If these ARE set:
#   - Real emails will be sent via SMTP
#   - Booking confirmations, verification codes, and notifications work
#
# For Gmail (recommended for small-scale):
#   1. Enable 2FA on your Google account
#   2. Create an "App Password": https://myaccount.google.com/apppasswords
#   3. Use the app password (NOT your regular password) below
#
# EMAIL_HOST="smtp.gmail.com"
# EMAIL_PORT="587"
# EMAIL_USER="your-email@gmail.com"
# EMAIL_PASS="your-16-char-app-password"
# EMAIL_FROM="WanderNest <noreply@wandernest.com>"

# Where contact form submissions should be sent
# If not set, defaults to EMAIL_FROM address
# CONTACT_EMAIL="support@wandernest.com"

# ========================================================================
# 🟡 OPTIONAL: VERIFICATION CODE SETTINGS
# ========================================================================
# How long verification codes are valid (in seconds)
# If not set, defaults to 600 seconds (10 minutes)
#
# VERIFICATION_CODE_EXPIRY="600"

# ========================================================================
# 🟡 OPTIONAL: REDIS/KV CONFIGURATION
# ========================================================================
# If NOT set:
#   - Verification codes stored in PostgreSQL database (slower but functional)
#   - App performance may be slightly slower for verification flows
#
# If set:
#   - Verification codes stored in Redis (much faster)
#   - Better performance for high-traffic scenarios
#
# For Vercel KV (recommended):
#   1. Go to your Vercel project → Storage → Create Database → KV
#   2. Copy the KV_URL or REDIS_URL from the .env.local tab
#   3. Paste it here
#
# For Upstash Redis:
#   1. Create account at: https://upstash.com/
#   2. Create a Redis database
#   3. Copy the connection string (starts with redis:// or rediss://)
#
# REDIS_URL=""

# ========================================================================
# 🟡 OPTIONAL: PUBLIC BASE URL
# ========================================================================
# The public URL where your app is deployed
# Used in email links, OAuth callbacks, and SEO metadata
#
# If NOT set:
#   - App will try to use NEXTAUTH_URL as fallback
#   - Email links may not work correctly
#
# If set:
#   - Email links will work properly
#   - Better SEO and social sharing metadata
#
# Example: https://wandernest.vercel.app (no trailing slash)
#
# NEXT_PUBLIC_BASE_URL=""

# ========================================================================
# 🟢 OPTIONAL: ADMIN FEATURES (JWT)
# ========================================================================
# Only needed if you plan to use admin panel features
# If not set, admin authentication will not work (but app will work fine)
#
# Generate with: openssl rand -base64 32
#
# JWT_SECRET=""

# ========================================================================
# 🔵 AUTO-SET BY VERCEL (DO NOT MANUALLY SET)
# ========================================================================
# These are automatically set by Vercel - do not add them manually:
# - NODE_ENV (set to "production" automatically)
# - VERCEL_URL (set to your deployment URL automatically)
# ========================================================================

# ========================================================================
# DEPLOYMENT CHECKLIST
# ========================================================================
# Before deploying, make sure you have:
#
# ✅ REQUIRED (app won't work without these):
#    [ ] DATABASE_URL - PostgreSQL connection
#    [ ] GOOGLE_CLIENT_ID - Google OAuth
#    [ ] GOOGLE_CLIENT_SECRET - Google OAuth
#    [ ] NEXTAUTH_SECRET - Session security
#    [ ] NEXTAUTH_URL - Your production domain
#
# ⚠️  RECOMMENDED (app works but with reduced functionality):
#    [ ] EMAIL_HOST, EMAIL_USER, EMAIL_PASS, EMAIL_FROM - Email sending
#    [ ] REDIS_URL - Faster verification code storage
#    [ ] NEXT_PUBLIC_BASE_URL - Correct email links
#
# ℹ️  OPTIONAL (nice to have):
#    [ ] CONTACT_EMAIL - Custom email for contact form submissions
#    [ ] VERIFICATION_CODE_EXPIRY - Custom verification timeout
#    [ ] JWT_SECRET - If using admin features
# ========================================================================
